// Compute Corpus — code, math, logic, information theory
// Eye 2's sense organ. A different domain for the brain to integrate.
// These are neurons — they compete in tournaments like everything else.

export const CORPUS_COMPUTE = [
  // Algorithms & computation
  'a function is a map from input to output',
  'the loop iterates until the boundary is reached',
  'recursive functions call themselves to solve',
  'sort the array then search for the element',
  'the algorithm reduces complexity through abstraction',
  'the stack grows and the stack shrinks',
  'every node connects to every edge through a path',
  'the tree has a root and the root has branches',
  'depth first or breadth first changes what you find',
  'compute the cost of each path to the minimum',

  // Logic & structure
  'if the state is true then return the value',
  'the set contains the element or it does not',
  'union and intersection are dual operations',
  'the complement of a subset is everything else',
  'binary logic reduces all to zero and one',
  'the type constrains what the value can be',
  'abstract the pattern then compose the method',
  'filter the noise to find the signal',
  'the domain maps to the range through the function',

  // Math & analysis
  'the derivative measures the rate of change',
  'the integral sums the continuous into the discrete',
  'converge to the limit or diverge to infinity',
  'the gradient points toward the direction of steepest descent',
  'the equation balances what the variable transforms',
  'linear growth is constant but exponential growth compounds',
  'the proof is the path from assumption to theorem',
  'probability is the weight of the unknown',
  'the ratio reveals the proportion between things',
  'the inverse undoes what the function does',

  // Information & learning
  'entropy measures the information in the signal',
  'the network learns by adjusting the weight of each layer',
  'precision and accuracy are not the same thing',
  'the model is a compression of the training',
  'the loss function measures distance from truth',
  'the threshold determines what activates and what does not',
  'the feature is what the network learns to see',
  'the bias shifts the origin of the decision boundary',
  'noise is not error it is information you cannot read yet',
  'the parameter is what survives the optimization',
]
